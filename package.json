{
  "name": "music-player",
  "version": "0.1.0",
  "private": true,
  "description": "Native local-first music player (Tauri + Rust). No YouTube stream extraction — see docs/COMPLIANCE.md.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
